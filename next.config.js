/** @type {import('next').NextConfig} */
const nextConfig = {
  // Static export — no backend, pure client-side
  // Remove this line if you want Vercel's default Node.js runtime
  // output: 'export',
}

module.exports = nextConfig
